<UserControl x:Class="MvvmSampleWpf.Views.IocView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MvvmSampleWpf.Views" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:mdxaml="clr-namespace:MdXaml;assembly=MdXaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadDocsCommand}" CommandParameter="Ioc"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <ScrollViewer Padding="16" >
        <StackPanel Margin="16">
            <mdxaml:MarkdownScrollViewer Markdown="{Binding Texts[Ioc (Inversion of control)], Mode=OneWay}"/>
            <mdxaml:MarkdownScrollViewer Markdown="{Binding Texts[Configure and resolve services], Mode=OneWay}"/>
            <mdxaml:MarkdownScrollViewer Markdown="{Binding Texts[Constructor injection], Mode=OneWay}"/>
        </StackPanel>
    </ScrollViewer>

</UserControl>
