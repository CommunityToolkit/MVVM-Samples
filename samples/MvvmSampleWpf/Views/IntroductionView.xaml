<UserControl x:Class="MvvmSampleWpf.Views.IntroductionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:mdxaml="clr-namespace:MdXaml;assembly=MdXaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             >
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadDocsCommand}" CommandParameter="Introduction"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <StackPanel >
        <mdxaml:MarkdownScrollViewer Markdown="{Binding Texts[Introduction to the MVVM package]}" />
        <mdxaml:MarkdownScrollViewer Markdown="{Binding Texts[When should I use this package?]}" />
    </StackPanel>

</UserControl>
